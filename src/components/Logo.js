import logo from "../assets/images/logo.svg"; //logo is the name we select

const Logo = () => {
  return <img src={logo} alt="jobster logo" className="logo" />;  //this is the J logo we have on the landing page
};
export default Logo;
